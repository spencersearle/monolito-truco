.DS_Store
__pycache__/

# Claude Code internal state / worktrees
.claude/

# Node / Capacitor
node_modules/
www/

# Capacitor copies the web assets into the native projects on `cap sync`;
# they're regenerated, so they don't need to be tracked.
ios/App/App/public/
android/app/src/main/assets/public/

# Native build output
*.apk
*.aab
*.ipa
